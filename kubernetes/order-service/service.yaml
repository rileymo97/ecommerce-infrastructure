apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ecommerce-dev
  labels:
    app: order-service
    environment: dev
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 3002
      targetPort: 3002
  type: ClusterIP